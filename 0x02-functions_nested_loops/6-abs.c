#include<stdio.h>
#include<stdlib.h>
#include"main.h"

/**
 * _abs -  function that computes the absolute value of an integer
 * @n: the abs_value
 * Return: Absolute value of n or 0
 */

int _abs(int n)
{
	return (abs(n));
}
